kind: Cluster
apiVersion: kind.x-k8s.io/v1alpha4
name: infergrid
nodes:
  - role: control-plane
    extraPortMappings:
      - containerPort: 30080   # maps to the NodePort in service-nodeport.yaml
        hostPort: 30080
        protocol: TCP
  - role: worker
  - role: worker